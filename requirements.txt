# Dev/build dependencies for working on flexicon locally.
# Source of truth is pyproject.toml's [project.optional-dependencies].
#   pip install -r requirements.txt   ==   pip install -e .[dev]
-e .[dev]
