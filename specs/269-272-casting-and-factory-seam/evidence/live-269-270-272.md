# Live LCM verification -- issues #269, #270, #271, #272

Date: 2026-09-08
Branch: spec/242-gate-and-name-field-identity
FieldWorks: C:\Program Files\SIL\FieldWorks 9
LibLCM: 11.0.0

## Commands run

```
$env:FLEXLIBS_REQUIRE_LIVE = "1"
python -m pytest tests/operations/test_issue269_resolve_object_live_target.py `
                 tests/operations/test_issue272_complex_form_live.py `
                 tests/operations/test_collection_cast_pattern.py `
                 tests/operations/test_lexentry_resolve_object_live.py `
                 -m requires_live_project -q -rs
```

Result: **6 passed, 1 skipped, 68 deselected, 6 errors**

`tests/live_status.json` -> `"run_mode": "live"`  (requirement 1 satisfied)

## PASS -- #272 complex-form write path (real Target, in place)

`tests/operations/test_issue272_complex_form_live.py`

1. `LexiconAddComplexForm` -- pre-state: fresh `TEST_blackboard`, `EntryRefsOS`
   traversal yields `[]`. Post-state: re-read by `ServiceLocator.GetObject(hvo)`
   (not the returned object) yields `["TEST_black", "TEST_board"]`. PASS
2. `AddComplexFormComponent` -- pre-state `[]` -> post-state `["TEST_sun"]`,
   and `component.Hvo` present in a re-read `PrimaryLexemesRS`. PASS

Pre-fix both raised `AttributeError: 'ILcmServiceLocator' object has no
attribute 'GetInstance'` and the UnitOfWork rolled back.

Target checked clean afterwards: 0 entries, 0 `TEST_` leftovers.

## PASS -- #269 __ResolveObject (Target sandbox, self-created data)

`tests/operations/test_issue269_resolve_object_live_target.py`

3. Defect 1 (non-int branch uncast) -- a component returned by
   `GetComplexFormComponents` fed to `GetHeadword` returns its headword
   text. Pre-fix: `AttributeError: 'ICmObject' object has no attribute
   'HeadWord'`. PASS
4. Defect 2 (HVO guard false negative) -- `GetHeadword(entry.Hvo) ==
   GetHeadword(entry)`, both re-read from the LCM. Pre-fix:
   `FP_ParameterError: HVO does not refer to a lexical entry`. PASS
5. Rejection preserved -- a real `ILexSense` HVO still raises
   `FP_ParameterError`. PASS

Note: #269 shipped `test_lexentry_resolve_object_live.py` against
`sena3_sandbox`, which cannot run here (see BLOCKED). This file re-proves
the same three assertions on a Target sandbox with data it creates, so
#269 is verified live rather than reported on a skip.

## PASS -- #270 Tier 1 round trip (Target sandbox)

`tests/operations/test_collection_cast_pattern.py::test_complex_form_component_round_trip`

6. `GetComplexFormComponents` output fed straight back into
   `AddComplexFormComponent`; post-state re-read from the LCM contains the
   component Hvo, and each component exposes `LexemeFormOA`/`SensesOS`.
   This is the reported production `AttributeError`, and the Get/Add
   composability failure. PASS

## Three further production defects found BY this live run

None were in any issue; all were unreachable until the #272 migration
unblocked the code path, or were masked by mocks.

1. `FLExProject.LexiconAddComplexForm` never set `RefType`. A fresh
   `ILexEntryRef` defaults to `0 == krtVariant`, so the documented
   FLExProject-level entry point filed components under a VARIANT
   reference and the entry was never a complex form;
   `GetComplexFormComponents` (which filters `krtComplexForm`) returned
   `[]`. Now sets `RefType = krtComplexForm` and `HideMinorEntry = 0`,
   matching the sibling `AddComplexFormComponent`. Pinned offline by
   `test_issue272_service_locator_seam.py` and live by item 3 above.
2. Three dead `.Append(` calls on `ILcmReferenceSequence<T>`, which
   exposes no such member (`FLExProject.py` 4913/4947/4951 pre-fix).
   Replaced with `.Add(`, the member used at 20+ live-verified sites.
   `LexiconSetComplexFormType` was the worst case: it did `Clear()` then
   the dead `Append()`, so calling it wiped the existing complex-form
   type and then raised -- data loss reachable in current releases, and
   it never depended on `GetInstance`.
3. The #272 seam mock modelled `.Append()`, so it passed against the dead
   production code. Stub corrected to model `.Add()`.

## BLOCKED -- needs_human

**6 errors, all `sena3_sandbox` unavailable.** No `Sena 3*.fwbackup` exists
in `tests/fixtures/` (only `Target 2026-07-06 0218.fwbackup`), though the
live `Sena 3` project is present in `C:\ProgramData\SIL\FieldWorks\Projects`.

Affected: `test_lexentry_resolve_object_live.py` (3, superseded by the
Target-based file above) and `test_collection_cast_pattern.py` Tier 3/4
read-path (3):
  - possibility items expose `IPartOfSpeech`-only surface
  - `GetSubitems(recursive=True)` reaches grandchildren
  - `ConstChartCellTags.GetAll(row)` is not empty when tags exist

So **#270 Tiers 3 and 4 are `FAIL: unverified`** -- their mechanism was
established offline only. Remedy: place a `Sena 3*.fwbackup` in
`tests/fixtures/` and re-run, or point these three at a Target sandbox
with created data.

**Also unverified:** `SetAudioPath`/`GetAudioPath` (#272's three
string-argument sites). Skipped -- the Target has no audio writing
system. Offline round-trip only.

## Test-infrastructure defect fixed

`conftest.sena3_sandbox` used bare `pytest.skip()` and ignored
`FLEXLIBS_REQUIRE_LIVE`, unlike `target_project`/`target_sandbox`, which
fail loudly. CLAUDE.md states the flag must convert "a missing fixture"
into a hard failure. It now does. Consequence: 23 pre-existing
sena3-dependent tests across 12 files now ERROR under the flag instead of
skipping green. That is the honest signal that they are unverified in this
checkout, not a regression -- it is why the 6 errors above are visible at
all.
