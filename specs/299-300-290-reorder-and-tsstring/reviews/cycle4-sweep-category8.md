## Pattern audit: bare ITsString field accessed as IMultiString (Category 8)
Original site: flexicon/code/Discourse/ConstChartRowOperations.py (Label/Notes)

Siblings found:
- D:\Github\_Projects\_LEX\flexicon\flexicon\code\Lexicon\LexEntryOperations.py:470  HIGH  `new_etym.Source.CopyAlternatives(etymology.Source)` in `LexEntryOperations.Duplicate` reads/writes `ILexEtymology.Source`, but `EtymologyOperations.py:368-374` documents (via live reflection, 2026-08-18) that `ILexEtymology` has **no `Source` field at all** — it was replaced by `LanguageNotes` (IMultiString). This call path was missed by the #290-adjacent fix that repaired `EtymologyOperations.Duplicate`/`Create`/`GetSource`/`SetSource`; it will raise `AttributeError` unconditionally the first time a duplicated `LexEntry` has an etymology.
- D:\Github\_Projects\_LEX\flexicon\flexicon\code\Notebook\NoteOperations.py:815-818 (`GetAuthor`)  HIGH  `note.Source.get_String(ws)` on an `ICmBaseAnnotation` — the same file's own `Duplicate` method (lines 362-368) documents, confirmed live, that "Source" was renamed/replaced by `SourceRA` (a reference-atomic field, not text) on this object, with pythonnet itself surfacing "Did you mean: 'SourceRA'?". `hasattr(note,"Source")` is almost certainly False, so `GetAuthor` silently always returns `""`.
- D:\Github\_Projects\_LEX\flexicon\flexicon\code\Notebook\NoteOperations.py:854-858 (`SetAuthor`)  HIGH  Same field/object as above; `note.Source.set_String(ws, mkstr)` — writes silently no-op (branch never entered) because the real field is `SourceRA`, not a settable ITsString/IMultiString `Source`.
- D:\Github\_Projects\_LEX\flexicon\flexicon\code\Notebook\NoteOperations.py:448-449 (`GetSyncableProperties`)  HIGH  `if hasattr(note,"Source"): props["Source"] = ITsString(note.Source.get_String(wsHandle)).Text` — same vanished/renamed `Source`→`SourceRA` field on `ICmBaseAnnotation`; syncable-properties export silently omits author data.
- D:\Github\_Projects\_LEX\flexicon\flexicon\code\TextsWords\DiscourseOperations.py:884-888 (`SetCellContent`, Label branch)  MED  `hasattr(cell,"Label")` → `cell.Label.set_String(wsHandle, content_str)`, where `cell` is a `ConstChartWordGroup`/`ConstChartTag`/`ConstChartClauseMarker`/`ConstChartMovedTextMarker` (from `row.CellsOS`), not the `IConstChartRow` whose `.Label` is confirmed bare ITsString (issue #290). Same duck-typed shape as the confirmed bug, but source alone can't confirm whether any real chart-cell-part subtype even exposes a `Label` member (none of the sibling `ConstChart*Operations.py` files reference one) — could be dead/unreachable code rather than a live type-confusion crash. (Cross-ref: cycle1 live evidence already showed this branch dead for real cells.)
- D:\Github\_Projects\_LEX\flexicon\flexicon\code\TextsWords\DiscourseOperations.py:889-893 (`SetCellContent`, Comment branch, task-cited as line 892)  MED  Same shape as above for `cell.Comment.set_String(...)`; cannot confirm from source whether the underlying chart-cell-part class exposes `Comment` as ITsString, IMultiString, or not at all.
- D:\Github\_Projects\_LEX\flexicon\flexicon\code\TextsWords\DiscourseOperations.py:948-950 (`GetCellContent`, Label branch)  MED  `ITsString(cell.Label.get_String(wsHandle)).Text` inside a `try/except (AttributeError, ...)` that swallows the failure — mirrors the confirmed-buggy `IConstChartRow.Label` access pattern but on a cell-part object whose actual field type/existence is unconfirmed from source; failure is masked (returns "") rather than crashing, consistent with either a real bug or genuinely-absent field.
- D:\Github\_Projects\_LEX\flexicon\flexicon\code\TextsWords\DiscourseOperations.py:957-959 (`GetCellContent`, Comment branch)  MED  Same shape as the line-950 Label case, for `cell.Comment.get_String(wsHandle)`.

Sites cleared / not enumerated: reviewed ~517 `.get_String`/`.set_String`/`BestAnalysisAlternative` occurrences across all `*Operations.py` + `BaseOperations.py`; the overwhelming majority (Name/Gloss/Form/Description/Comment/Abbreviation/etc. on LexEntry, LexSense, Person, Location, Etymology(Form/Gloss/Comment/Bibliography), Pronunciation, Allomorph, Wordform, WfiGloss, SemanticDomain, ReversalIndex(Entry), Publication, ConstChartMarker, etc.) match well-established, already-consistent IMultiString usage and show no duck-typing/hasattr red flags — cleared. `TextOperations.py:305-363` (`text_obj.Source`, `IText.Source`) uses `CopyAlternatives`/`GetMultiStringDict` consistently and is a distinct, independently-documented MultiString field from `IStText.Source`/`ICmBaseAnnotation` — cleared, not the same trap. `PhonemeOperations.py` (`BasicIPASymbol`) and `TextsWords/SegmentOperations.py` + `ParagraphOperations.py` (`BaselineText`) already implement the correct bare-ITsString/dual-shape handling (issues #222 and prior BaselineText fix) — cleared, already fixed.
