{
  "feature": "243-closeproject-save-guard",
  "campaign": "tier1-silent-data-loss",
  "campaign_index": 1,
  "issues": [
    243
  ],
  "status": "needs_human",
  "last_cycle": 5,
  "spurts_completed": 5,
  "checkpoint_reached": "CP-2c HALF REACHED (spurt 5, cycle 5): T6 DONE, T7 still blocked. T6 ran as a ONE-SHOT on the user's explicit greenlight of T6 and T6 only -- NOT inside the loop, so nothing could drift into T7/T5. git diff --stat -- flexicon/ is EMPTY (re-verified by /lex-lead): zero behaviour change, so T6 stayed ruling-independent as designed. Probe file 6 -> 9 live tests (9 'def test_' functions confirmed by grep), all green with run_mode: live; test_undoable_mode_live.py 33/33; test_target_live_smoke.py 3/3; offline 1290 passed with deselected 470 -> 473 (exactly the +3 new live tests). CP-B defect 2 CLOSED: test_p5_save_before_forced_end now asserts close_exc_msg is None (probe line 575) and the [PROBE][P5] lines are quoted verbatim in the evidence file, so C13 fact 3 is a measurement, not prose. Four rulings followed: C16 (mechanism ANSWERED -- (ii) CONFIRMED, (i) RULED OUT, (iii) indistinguishable from (ii) and left unresolved BY DESIGN), C17 (#243's CEILING -- the single most important fact in this feature), C18 (T7's detector re-scoped; supersedes C14 point 3), C19 (T5 SPLIT into T5a/T5b). Still needs_human: the user's coupled ruling is outstanding and is now the ONLY thing that can fix the filed incident.",
  "checkpoint_numbering_note": "The campaign QUEUE.md called the spec+probe milestone 'Checkpoint 1'; it is DONE. tasks.md's three implementation checkpoints are CP-A (T1-T2), CP-B (T3-T4), CP-C (T5). As of spurt 2, CP-A is further SPLIT into CP-A1 = T1 (DONE) and CP-A2 = T2 (NEXT). Do not read tasks.md's legacy 'Checkpoint 1' heading as un-done work; T1's checkbox is ticked.",
  "tasks_done": [
    "T1",
    "T3",
    "T4",
    "T6"
  ],
  "next_checkpoint": "T7 (the C14/C18 loudness remedy) -- BLOCKED on the user's coupled ruling. Then T5b (the prominent release note + SaveChanges() docstring correction + Q4), also gated. SEPARABLE AND RECOMMENDED, ruling-independent: T5a, a minimal [Unreleased] CHANGELOG stub noting T3 only, scoped to C17's two halves (spec.md C19). T5a is OFFERED as a second pre-authorised unit, exactly as T6 was -- the USER greenlights it or not.",
  "next_entry": "DO NOT RESUME AUTONOMOUSLY, AND THERE IS NO LOOP TO RESUME -- the ralph loop is CANCELLED. T6, the one pre-authorised exception, is now DONE, so nothing in this feature is startable without the user. THE BLOCKER IS THE ONLY REMAINING GATE and it has been SHARPENED by T6: per C16/C17 the SaveChanges() depth guard is now MEASURED to be the ONLY thing that can fix the incident #243 was filed about (the change set is gone before CloseProject() is entered), so the user is not deciding whether to approve an extra improvement -- they are deciding whether flexicon fixes #243's incident at all, or ships #243 documenting it as unfixed. AFTER the ruling: T7 (Phase-1 else: branch logs at ERROR not debug; always attempt usm.Save() then raise FP_ProjectError when the save cannot be trusted; detector per C18 -- the envelope-missing anomaly is PRIMARY and the post-Save() HasUnsavedChanges read is REJECTED as indistinguishable; Dispose()/del self.project into a finally per C15; re-point T4's P-5 assertions at 'CloseProject() RAISES, survivors stay 0/25' -- and per C17 that 0/25 is the CEILING, not a pending improvement). Then T5b. Do NOT start T5b first. Do NOT re-run T6 (done, evidence on file). Do NOT touch SaveChanges() in any task. Do NOT re-attempt T2 (C12). Do NOT reopen C1-C19, Q1, Q2, Q3 or Q5's detector half. Do NOT file GitHub issues.",
  "contract_frozen": "C1-C19 in spec.md. Do not reopen. C16-C19 are new in spurt 5. NOTE TWO SUPERSESSIONS: C18 supersedes C14 point 3 (which preferred a direct post-Save() IUndoStackManager read -- now measured indistinguishable and void), and C16/C17 ANSWER C13's 'NOT frozen' paragraph (read that original as the historical statement of the open question). Overturning C11-C19 requires citing the decision explicitly.",
  "key_rulings": {
    "C1": "Fix shape is a GUARD around the line-326 EndNonUndoableTask() call, NOT the reorder the issue prose proposed. P-5 proved usm.Save() itself raises 'Commit at wrong place.' at CurrentDepth > 0 and collapses the envelope, so swapping lines 326/332 trades one guaranteed raise for another with no save either way.",
    "C6": "P1 is a hard PREREQUISITE of P0: the guard checks depth first and only calls EndNonUndoableTask() when an envelope is open, then still wraps the call so any other raise cannot skip Save().",
    "C7": "End-then-Save order is unchanged.",
    "C9": "Q1 RECONCILED. The owner's incident is a P-5 -> P-3 CHAIN: mid-session SaveChanges() under undoable=False hits usm.Save() at depth 1, raises the owner's exact 'Commit at wrong place.' string and collapses the envelope; the collapsed envelope then makes CloseProject()'s unguarded End raise, skipping Save(), losing the whole in-memory session. The C6 guard breaks the chain. The trigger is reachable from flexicon's own SHIPPED SaveChanges() docstring Example (FLExProject.py:563-588), correct under undoable=True and a guaranteed raise under undoable=False -- so no owner repro steps were needed.",
    "C10": "The .fwdata file-swap detail is OUT OF SCOPE and must not be claimed as fixed. CloseProject() has no path that renames/rotates/replaces .fwdata; the probe's delta-0 result is the correct consequence of Save() never running. Attributed to FieldWorks-side recovery, left unexplained by design.",
    "C11": "HasOpenSessionTask() reads depth BEFORE the self._undoable mode check. The cycle-2 dispatch brief's 'return False WITHOUT consulting depth' wording was WRONG and is superseded. Two independent reasons: (1) C4 requires closed/never-opened to raise FP_ProjectError from BOTH members with no mode carve-out -- short-circuiting first would make a closed undoable=True project answer False instead of raising, the exact silent-answer ambiguity P1 removes; (2) FLExProject has no __init__ and OpenProject() assigns self.project (line ~263) BEFORE self._undoable (line 271), so on a never-opened instance testing self._undoable first raises a bare AttributeError instead of the documented FP_ProjectError. C3 is NOT weakened: depth is read for its raise-or-not effect only and discarded unread on the Phase 2 branch, so the ANSWER is still never depth-derived under undoable=True. Do not 'fix' this back to the brief wording; overturning it requires citing C11 explicitly.",
    "C12": "T2 DROPPED, not deferred, and not merely on cost. The premise was wrong: _ReadActionHandlerDepth() is deliberately STRICT (raises FP_ProjectError, returns depth verbatim -- C5) while transaction.py:_current_depth / undoable_operation.py:102 / CustomFieldOperations.py:306 are deliberately LENIENT (coerce non-int to 0 so a malformed double degrades to 'treat as outermost'). One implementation cannot serve both contracts; sharing them is a conflation, not a de-duplication. Escape hatch 1 (isinstance(depth,int) after delegation) REJECTED: behaviour-preserving in production but it makes the doubles stop testing what they claim to, and decisively the prescribed except-wrapper would swallow the FP_ProjectError the helper exists to raise and substitute 0 -- at CustomFieldOperations.py:306 that silently disables the issue-#21 corruption guard. Escape hatch 2 (edit the static source-grep test at tests/test_custom_field_create_refusal.py:54-60) REJECTED: it deliberately pins that corruption guard's implementation; loosening it for a zero-functional-delta refactor is net-negative, and parking the literals in a comment would be gaming it. Escape hatch 3 (add spec= to the doubles) REJECTED as a means to T2 and NOT owed as a follow-up, since T2 itself is dropped. C5's 'optionally at the implementer's discretion during T2' clause is now CLOSED as declined. Nothing in P0/P1/P2 depends on T2.",
    "C13": "What the T4 P-5 measurement DOES and DOES NOT establish. T3's guard is CONFIRMED CORRECT and needs no rework; C1/C6/C7 stand. FROZEN: (1) the rival 'the guard skipped an End that should have run' hypothesis -- which would implicate the fix SHAPE -- is DISPROVED by measurement in the same run: the probe force-calls EndNonUndoableTask() manually before CloseProject() and that forced call STILL raises 'Cannot end task that has not been started.', so the envelope was genuinely gone and an End could not have succeeded either way; (2) black box, live: after a failed mid-session SaveChanges() at depth 1, CloseProject() reaches usm.Save() at depth 0, does NOT raise, and 0/25 persist; (3) therefore usm.Save() RETURNED SUCCESSFULLY HAVING PERSISTED NOTHING -- a sound inference (the usm.Save() call is bare, so a raise would have propagated) but NOT a direct measurement, and it rests on (2)'s 'did not raise' which T4 recorded in prose but never asserted; T6/P-9 must pin it. NOT FROZEN, do not cite as settled: 'the UnitOfWorkService cannot commit after a failed CheckReadyForCommit'. That is a liblcm-INTERNALS claim inferred from one black-box survivor count. Three rival mechanisms produce identical observations: (i) the UOW/UndoStack is poisoned session-wide; (ii) SaveChanges()'s failure path DISCARDED the pending change set, so the loss already happened before CloseProject() was entered and NO CloseProject()-side change could ever reach 25/25; (iii) collapsing the envelope unregistered the dirty objects so usm.Save() correctly flushed an empty change set. Distinguishing (ii) is decisive for #243's ceiling -- routed to T6, not into the contract. Bearing on the user's ruling: all three mechanisms imply the SAME shape for the fourth ask (make SaveChanges() refuse usm.Save() at CurrentDepth > 0), so the uncertainty does not block the decision to approve -- it only bounds what #243 may claim without it.",
    "C14": "T3's quiet path is a NEW SILENT-LOSS SURFACE, and closing it is IN SCOPE for #243. For the owner's actual P-5 -> P-3 chain (C9): BEFORE T3, 0/25 lost and CloseProject() RAISED; AFTER T3, 0/25 lost and CloseProject() RETURNS NORMALLY emitting one debug line. Identical loss, and the only close-path signal was removed. On the P-3 path (stray/forced End with an INTACT change set) T3 is a genuine complete fix (25/25) and that stands unqualified -- but on the path C9 names as the owner's REAL incident T3 is an OBSERVABILITY REGRESSION, and observability is the filed complaint verbatim ('the only symptom logged was a single [WARN] Commit at wrong place.'). Post-T3 the close path is quieter than that: we did not make the loss worse, we made the SILENCE worse, on precisely the path this campaign is named after. THE INSTRUCTION WAS WRONG, NOT THE IMPLEMENTATION: tasks.md T3 said 'log at debug level' and the programmer followed it exactly, but in Phase 1 (writeEnabled and not _undoable) a False from HasOpenSessionTask() inside CloseProject() is ANOMALOUS BY CONSTRUCTION (the envelope opens at OpenProject() and the frozen P-2 table shows depth holds at 1 all session), so that branch means we are standing inside the owner's incident. Same class as C11: a /lex-lead brief-wording error. REMEDY = T7, in flexicon/code/FLExProject.py only, SaveChanges() NOT touched: (1) the Phase-1 else: branch logs at ERROR not debug; (2) CloseProject() still ALWAYS attempts usm.Save() (C1/C6 stand, never skip it) and then REFUSES to return normally when it has detected the save cannot be trusted -- raise FP_ProjectError saying explicitly that the session's changes may not have been written to disk; attempt everything, THEN fail loudly; (3) detector chosen by T6/P-9, preferring a direct IUndoStackManager still-has-unsaved-changes read (mechanism-independent) over the envelope-missing heuristic; (4) re-point T4's P-5 assertions at the new contract. T7 RESTORES LOUDNESS, NOT DATA: it makes total loss impossible to miss but cannot make the owner's sequence save -- only the queued SaveChanges() fourth ask can. T5's CHANGELOG must say exactly that and must not overstate it (same discipline as C10).",
    "C15": "Q2(a) RESOLVED: Dispose() moves into a try/finally. Q2's original question was correctly left OPEN by T3 -- T3's scope genuinely did not force an answer and it was not silently decided in spurts 1-3. C14 forces it: T7 makes CloseProject() raise after attempting usm.Save(), and a raise emitted there must not leak the live LCM handle, so self.project.Dispose() / del self.project move into a finally. Resolved as a consequence of C14, not deferred a fourth time. The probe harness's hand-rolled _dispose_if_open() teardown becomes belt-and-braces rather than load-bearing -- leave it in place.",
    "C16": "C13's mechanism question is ANSWERED by T6. (ii) CONFIRMED by MEASUREMENT: P-7 re-read the 25 TEST_p7_ entries from the STILL-OPEN project immediately after SaveChanges() raised at CurrentDepth == 1, before CloseProject() was ever called -- 0/25, already absent from LexEntry.GetAll(). (i) RULED OUT: P-8's fresh post-failure BeginNonUndoableTask()/End pair committed its own new entry 1/1 in the same process, so the UnitOfWorkService is NOT globally poisoned -- which also CONTRADICTS the cycle-4 report's asserted 'cannot commit after a failed CheckReadyForCommit' wording. (iii) NOT separately distinguishable from (ii) by anything measured: both name the same loss point and the same scope conclusion, so it is LEFT UNRESOLVED BY DESIGN -- do NOT upgrade this to '(ii) proven and (iii) excluded'. Splitting them would need instrumentation inside liblcm, outside this project's reach (same disposition as C10).",
    "C17": "#243's CEILING -- THE SINGLE MOST IMPORTANT FACT IN THIS FEATURE, and it has TWO halves that must ALWAYS be stated together. HALF ONE: because the change set never survives as far as CloseProject() (C16), NO CloseProject()-side change -- not T3's shipped guard, not T7, not any future guard, wrapper, retry, reorder or try/finally in that method -- can EVER recover the owner's real P-5 -> P-3 sequence. The only place a fix for the filed incident can live is SaveChanges() itself, i.e. the campaign's still-unruled fourth ask. HALF TWO, unqualified: T3 IS a genuine, complete, shipped, live-verified fix for the independent P-3 path (intact change set + stray/forced End), measured 0/25 -> 25/25 with no raise at CP-B; C17 does not qualify, weaken or reopen it. Stating half one alone undersells verified work; stating half two alone is the C10-class overclaim.",
    "C18": "T7's DETECTOR, resolving Q5's genuinely open part and SUPERSEDING C14 point 3. Measured at T6/P-9: HasUnsavedChanges exists on the live UnitOfWorkService but read immediately AFTER usm.Save() it is False in BOTH the real-save (25/25 persisted) and no-op (0/25) cases -- INDISTINGUISHABLE, so the post-save read is REJECTED as a detector and C14's preference for it is void. PRIMARY AND REQUIRED: the Phase-1-envelope-missing anomaly from T1's HasOpenSessionTask() surface (accepted exactly as the T6 report recommended). OPTIONAL, DIAGNOSTIC ONLY: a pre-Save() HasUnsavedChanges read logged inside that already-anomalous branch -- it MUST NOT gate the raise and MUST NOT create a second code path, because (a) it is not independent information (P-9 read True only AFTER a successful End, so it is a proxy for 'did an End just succeed', which CloseProject() already knows first-hand from its own End attempt) and (b) it is not proven mechanism-independent, since (i) was ruled out in the same run. WORDING REJECTED: HasUnsavedChanges == False must NOT be worded as 'there is nothing pending to save' -- proven false-negative, since in P-9's no-op shape it read False before the trigger SaveChanges() while all 25 entries still existed in memory. The property means 'a completed-but-unsaved unit of work is registered', not 'dirty data exists'.",
    "C19": "T5 is SPLIT, because deferring all of it was re-costed and found NOT to be neutral: CHANGELOG.md has a live [Unreleased] section and T3 is ALREADY committed to main inside that window with NO entry at all, so a version cut today would ship an undocumented behaviour change to a public method, C14's observability regression included. T5a (RECOMMENDED, RULING-INDEPENDENT): a minimal [Unreleased] stub noting T3 only, scoped to C17's two halves -- the P-3 path is fixed 0/25 -> 25/25, the SaveChanges()-at-depth>0 path is NOT fixed and CloseProject() currently RETURNS NORMALLY there. That describes shipped behaviour, so it takes no position on the fourth ask and discloses nothing public issue #243 does not already state; churn cost is ZERO because the entry can be edited before any version cut. Constraints: C10 (no .fwdata claim), C14 (loudness not data), C17 (both halves). Docs-only, live-exempt. OFFERED as a second pre-authorised unit exactly as T6 was -- the USER greenlights it, the loop never does. T5b (the prominent P2 release note + the SaveChanges() docstring correction + Q4's placement/wording) STAYS GATED behind T7 and the ruling."
  },
  "open_questions": {
    "Q1": "CLOSED 2026-09-07 by /lex-lead -- see C9/C10. Do not reopen; no task may wait on owner repro steps.",
    "Q2": "RESOLVED 2026-09-07 (spurt 4) as C15: yes, Dispose() moves into a try/finally, forced by C14's new raise. Legitimately still open after T3. NOTE: cycle 4 filed the P-5 0/25 finding as a second dated note under Q2; /lex-lead ruled it MISFILED and MOVED it to Q5 -- Q2 asks what happens if usm.Save() RAISES, whereas the P-5 finding is the complement (it does not raise and silently persists nothing). Opposite branch, different remedy.",
    "Q3": "CLOSED 2026-09-07 by /lex-lead at T1: NO capability token. HasOpenSessionTask()/CurrentDepth do NOT join flexicon.CAPABILITIES; flexicon/__init__.py and tests/write_path_transactions/test_capabilities.py are OUT OF SCOPE for every remaining task. A plain additive read member is detectable with hasattr(project, 'HasOpenSessionTask'), so a token would add a second source of truth and widen T1's diff past the one-file additive scope fence. Recorded in spec.md under Q3 and in tasks.md's header, not merely in a dispatch rationale. Only reopening condition: a real consumer that cannot use hasattr (e.g. a wire-protocol client) -- route that to QUEUE.md as a NEW ask needing user approval, do not absorb it here. Verified honoured: neither file is in T1's changed-file list.",
    "Q4": "OPEN -- exact CHANGELOG placement and wording. /lex-doc's call at T5, which is now re-gated to LAST (after T6 and T7). Two hard constraints added by spurt 4: it must NOT claim #243 fixes the owner's filed incident (C9's P-5 -> P-3 chain still measures 0/25 per C13), and it must not overstate T7 either (C14: loudness, not data).",
    "Q5": "NEW, spurt 4. usm.Save() returns SUCCESSFULLY having persisted nothing -- how must CloseProject() detect and report that? Not open: the facts (frozen as C13), the severity and remedy shape (ruled as C14), the mechanism (routed to T6, must not be cited as settled until T6 reports). GENUINELY OPEN: the DETECTOR -- does the live IUndoStackManager expose a usable 'still has unsaved changes' read that would let CloseProject() detect a no-op save directly rather than inferring it from the Phase-1-envelope-missing anomaly? T6/P-9 answers it; T7 implements whichever answer comes back; a direct read is strongly preferred because it is mechanism-independent. ALSO gated on the user's ruling -- see 'blocker'."
  },
  "artifacts": {
    "spec": "specs/243-closeproject-save-guard/spec.md",
    "tasks": "specs/243-closeproject-save-guard/tasks.md",
    "status": "specs/243-closeproject-save-guard/STATUS.md",
    "probe_harness": "tests/operations/test_issue243_closeproject_probe.py",
    "evidence": [
      "specs/243-closeproject-save-guard/evidence/live-cycle1-probe.md",
      "specs/243-closeproject-save-guard/evidence/live-t1-depth-read-surface.md",
      "specs/243-closeproject-save-guard/evidence/live-t2-internal-callsite-dedup.md",
      "specs/243-closeproject-save-guard/evidence/live-t4-p0-guard-regression.md"
    ],
    "reviews": [
      "specs/243-closeproject-save-guard/reviews/cycle1-programmer.md",
      "specs/243-closeproject-save-guard/reviews/cycle1-domain.md",
      "specs/243-closeproject-save-guard/reviews/cycle1-archivist.md",
      "specs/243-closeproject-save-guard/reviews/cycle2-programmer.md",
      "specs/243-closeproject-save-guard/reviews/cycle3-programmer.md",
      "specs/243-closeproject-save-guard/reviews/cycle4-programmer.md"
    ]
  },
  "live_evidence": {
    "run_mode": "live",
    "spurt3_note": "T2 produced no code change, so there was nothing new to live-verify. Offline 1290 passed BEFORE and AFTER (identical to the pre-T1 baseline); test_undoable_mode_live.py 33 passed with run_mode: live; test_transaction_rollback.py collected 0 live tests (offline file); test_custom_field_multistring_best_alt.py 1 skipped (pre-existing env skip).",
    "t1_tests_passed": "6/6 (extended issue243 probe, cycle 2)",
    "offline_suite": "1290 passed, 470 deselected -- identical to the pre-T1 baseline, through both T1 and T2",
    "fixture": "target_sandbox_path (tempdir copy of the Target .fwbackup)",
    "real_target_touched": false,
    "restore_script_run": false,
    "spurt4_note": "T3+T4 live-verified, run_mode: live, target_sandbox_path fixture ONLY -- the real Target was never opened and no scripts/restore_*.py was run. probe 6/6, test_undoable_mode_live.py 33/33, test_target_live_smoke.py 3/3; test_transaction_rollback.py 20/20 OFFLINE (zero live markers); full offline suite 1290 passed, unchanged before/after the T3 patch. All four counts derived with '--collect-only -q -m requires_live_project' and pasted into the evidence file per the spurt-3 rule. Measured: P-3 0/25 -> 25/25 no raise (.fwdata 5,158,956 -> 5,179,581 bytes, +20,625, Target.bak sibling appeared); P-4 control 25/25 unchanged; P-5 0/25 with CloseProject() no longer raising. GAP, to be closed by T6/P-9: P-5's 'CloseProject() did not raise' is NOT asserted by the test (close_exc_msg is captured and never checked, unlike P-3) and the evidence file transcribes pass-counts rather than the [PROBE][P5] console lines from its -s run. That claim is load-bearing for C13 fact 3."
  },
  "code_modified_under_flexicon_code": true,
  "github_issues_filed": [],
  "deferred_needs_user_approval": [
    "SaveChanges() has no depth guard: under undoable=False it reaches usm.Save() at CurrentDepth=1, raising liblcm's 'Commit at wrong place.' AND collapsing the session envelope -- reachable straight from its own shipped docstring Example. Making it fail fast (or handle depth > 0) is a BEHAVIOUR change and a fourth ask beyond spec.md section 3's three, so it needs user approval to file/scope. In-scope mitigation already assigned to T5: correct the docstring Example (prose only)."
  ],
  "blocker": "ONE USER DECISION, TWO COUPLED PARTS -- and T6 has SHARPENED it. (a) Does FLExProject.SaveChanges() get a CurrentDepth guard so it fails fast instead of calling usm.Save() at depth > 0? (b) Given that answer, should CloseProject() raise when it detects a save it cannot trust (spec.md C14/C18, task T7)? WHAT T6 CHANGED: before T6, (a) read as an ask that 'would also help'. Per C16/C17 it is now MEASURED to be the ONLY thing that can fix the incident #243 was filed about -- P-7 read 0/25 from the still-open project before CloseProject() was ever entered, so #243's own three asks cannot fix it at any price. THE USER IS THEREFORE NOT DECIDING WHETHER TO APPROVE AN EXTRA IMPROVEMENT; THEY ARE DECIDING WHETHER FLEXICON FIXES THE #243 INCIDENT AT ALL, OR SHIPS #243 DOCUMENTING IT AS UNFIXED. The coupling is unchanged but the decline branch is heavier: approve (a) and the envelope is never collapsed, the chain never forms, the owner's sequence can reach 25/25, and T7's raise is near-unreachable defensive code; decline (a) and the incident is PERMANENTLY unfixable by measurement, with T7's ERROR + raise the entire remedy the owner ever gets, so its severity and wording matter a great deal. ONE OBSERVATION, NOT A NEW ASK: C16 places the loss INSIDE SaveChanges(), so even T7's raise reports it late -- if the 'fail fast / prevent' shape is declined, the honest maximum available is 'loud at close, already lost at SaveChanges()'. Whether that same fourth ask should therefore have a MINIMUM shape (report loudly at the point of loss) is part of the ruling about that one method; it is NOT a fifth ask and nothing may implement, prototype or plan it. Good news for the decision, unchanged: all mechanisms imply the SAME shape for (a) -- refuse usm.Save() at CurrentDepth > 0, failing fast before any damage.",
  "updated": "2026-09-07",
  "code_diff_scope": "SPURT 5 (T6): git diff --stat -- flexicon/ is EMPTY -- ZERO behaviour change, independently re-verified by /lex-lead, so T6 was ruling-independent as designed. Changed/added: tests/operations/test_issue243_closeproject_probe.py (+578, now 9 'def test_' functions), specs/243-closeproject-save-guard/evidence/live-t6-noop-save-mechanism.md (new), specs/243-closeproject-save-guard/reviews/cycle5-programmer.md (new), plus this spurt's spec.md / tasks.md / STATUS.md / handoff-json updates. Record note: .claude/ralph-loop.local.md is DELETED in the working tree -- that is the cancelled ralph loop, not a flexicon or spec artifact. Cumulative flexicon/ diff for the feature is unchanged from spurt 4: FLExProject.py, T1 (+120 additive) and T3 (+52, the CloseProject() guard). SaveChanges() remains provably UNMODIFIED.",
  "public_surface_added": {
    "_ReadActionHandlerDepth()": "private helper; raises FP_ProjectError when not hasattr(self, 'project'), else returns ActionHandlerAccessor.CurrentDepth verbatim. No lenient fallback (C5).",
    "CurrentDepth": "PROPERTY (not a method) -- matches the Cache property precedent. Raw int passthrough.",
    "HasOpenSessionTask()": "method. Reads depth first (C11), then False under _undoable (C3), else depth > 0."
  },
  "tasks_dropped": [
    "T2 -- DROPPED, not deferred. See spec.md C12. Do NOT re-attempt or re-tick as open work."
  ],
  "verification_plan_correction": {
    "defect": "The cycle-3 (T2) brief named live suites BY FILENAME. tests/operations/test_transaction_rollback.py carries ZERO requires_live_project markers -- 0 collected, 20 deselected -- so one of the three named 'live suites' verified nothing; test_custom_field_multistring_best_alt.py env-skipped (1 skipped). Only test_undoable_mode_live.py (33 passed) exercised anything.",
    "rule": "Never name a live suite by filename. Derive it with 'python -m pytest <file> -m requires_live_project --collect-only -q' and paste the count into the evidence file. 'no tests collected' is a ZERO, never a pass.",
    "live_counts_verified_2026_09_07": {
      "tests/operations/test_issue243_closeproject_probe.py": 6,
      "tests/operations/test_undoable_mode_live.py": 33,
      "tests/operations/test_target_live_smoke.py": 3,
      "tests/operations/test_transaction_rollback.py": "0 -- OFFLINE FILE, run with -m 'not requires_live_project' (20 tests)",
      "tests/operations/test_custom_field_multistring_best_alt.py": "1, env-dependent skip here -- not a usable gate"
    },
    "t3_t4_gate": "probe (6, grows with T4) + undoable_mode_live (33) + target_live_smoke (3) live, plus offline suite 1290 passed"
  },
  "test_hygiene_observation": "NON-BLOCKING, not a task, not an open question, no issue filed (recorded in spec.md C12): the project doubles in tests/test_b1t_action_handler_double.py, tests/operations/test_transaction_rollback.py and tests/test_custom_field_create_refusal.py are bare Mock()/MagicMock() with no spec=, so they auto-vivify any attribute and cannot detect a call-site change that reroutes a read through a new wrapper method. Latent blind spot in those three files only. A future feature may tighten them on its own merits; #243 does not.",
  "cp_b_defects_found_in_review": [
    "P1 -- T3's brief was wrong, not its implementation: 'log at debug level' made the Phase-1 no-envelope branch (anomalous by construction; it means we are standing inside the owner's incident) the quietest thing in the method. Frozen as C14, remedied at T7. Same class as C11; /lex-lead's defect to own.",
    "P1 -- T4's P-5 'CloseProject() did not raise' is UNASSERTED and UNQUOTED: test_p5_save_before_forced_end captures close_exc_msg and never checks it (test_p3_p6_reproduction_and_symptom asserts close_exc_msg is None at line 434), and the evidence file transcribes pass-counts rather than the [PROBE][P5] console lines from its -s run. Load-bearing for C13 fact 3 -- the whole difference between a silent no-op and a loud raise. Pinned by T6/P-9.",
    "P2 -- test_p5_save_before_forced_end now hard-asserts surviving_count == 0, i.e. it ENFORCES a P0 data loss as expected behaviour. The reasoning is sound (assert the measured value so a partial-write regression cannot pass green) and is accepted, but T7 must re-point it at the new contract, and whoever lands the SaveChanges() guard MUST flip it to 25 rather than read the green as 'the fix works'."
  ],
  "task_order_changed_spurt4": "T5 (CHANGELOG) was third of three; it is now LAST. New order: T6 (probe, ruling-independent, pre-authorised) -> T7 (the C14 loudness remedy, BLOCKED on the user's ruling) -> T5. tasks.md gained 'Checkpoint 2c' for T6-T7 and T5's Checkpoint 3 heading is re-gated. T6/T7 were APPENDED rather than renumbered so every existing cross-reference to T1-T5 stays valid.",
  "loop_status": "CANCELLED. The ralph-loop Stop hook is gone (.claude/ralph-loop.local.md deleted in the working tree) and will NOT re-feed a standing prompt. Nothing in this feature resumes automatically. A human must (1) rule on 'blocker', optionally (2) greenlight T5a per spec.md C19, then (3) restart the campaign loop with the standing prompt from lex-lead.md, completion promise TIER1 COMPLETE (never FEATURE COMPLETE). See STATUS.md -> 'How a human restarts this'.",
  "cp_2c_t6_outcome": {
    "verdict": "PASS, accepted by /lex-lead with claims independently re-verified (empty flexicon/ diff, 9 probe test functions, close_exc_msg assertion present at probe line 575).",
    "mechanism": "(ii) CONFIRMED (P-7: 0/25 in-memory from the STILL-OPEN project before CloseProject()); (i) RULED OUT (P-8: fresh post-failure envelope commits 1/1); (iii) INDISTINGUISHABLE from (ii), left unresolved BY DESIGN -- the report volunteered that limit itself rather than guessing, which is the correct outcome.",
    "ceiling": "SETTLED as C17. No CloseProject()-side change can ever fix the owner's P-5 -> P-3 sequence. T3's independent P-3 fix (0/25 -> 25/25) stands as shipped. Both halves must always be stated together.",
    "detector": "HasUnsavedChanges after usm.Save(): False in BOTH the real-save and no-op cases -- INDISTINGUISHABLE, unusable alone. Before the save: True vs False -- distinguishable but NOT proven mechanism-independent, and per C18 not independent information either (it is a proxy for 'did an End just succeed'). Ruling: envelope-missing anomaly PRIMARY; pre-Save read diagnostic-only; the 'nothing pending to save' wording REJECTED as a proven false-negative.",
    "cp_b_defect_2": "CLOSED. assert close_exc_msg is None added to test_p5_save_before_forced_end (probe line 575, confirmed in source); [PROBE][P5] console lines quoted verbatim in the evidence file. C13 fact 3 is now a measurement, not prose.",
    "gates": "probe 9/9 live (run_mode: live), test_undoable_mode_live.py 33/33, test_target_live_smoke.py 3/3, offline 1290 passed with deselected 470 -> 473 (exactly +3).",
    "evidence": "specs/243-closeproject-save-guard/evidence/live-t6-noop-save-mechanism.md",
    "report": "specs/243-closeproject-save-guard/reviews/cycle5-programmer.md"
  },
  "task_order_spurt5": "T6 DONE. Remaining: T7 (BLOCKED on the ruling) -> T5b (gated behind T7 + the ruling). T5 was SPLIT this spurt (C19): T5a, a minimal [Unreleased] stub, is ruling-independent and RECOMMENDED, and is OFFERED as a second pre-authorised unit for the user to greenlight -- it does NOT unblock T7 or T5b. T6/T7/T5a/T5b were appended/split rather than renumbered so every existing cross-reference to T1-T5 stays valid."
}
