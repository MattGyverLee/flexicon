{
  "campaign": "tier1-silent-data-loss",
  "kind": "campaign",
  "status": "in_progress",
  "completion_promise": "TIER1 COMPLETE",
  "promise_condition": "all four queue items status=done, gates green, live evidence on file. A per-feature feature_complete from lex-lead does NOT satisfy this.",
  "loop_status": "CANCELLED. The ralph-loop Stop hook was removed at iteration 5 of 40 on item 1's needs_human handoff. Nothing re-enters on its own; the main session drives lex-lead spurts directly on explicit user authorisation, one queue item at a time.",
  "active_index": null,
  "active_feature": null,
  "next_entry": "NOTHING IS ACTIVE. Sub-item 2a closed and approved 2026-09-07 (cycle 6). The campaign promise TIER1 COMPLETE is NOT satisfied and must not be emitted: items 3 and 4 remain. Item 3 (feature-structure-sync-gap, #251/252/253/256) is RESUME-not-replan -- its own .crew-handoff.json next_entry is authoritative, and note the SECOND CREW has been working it concurrently in this same clone and has advanced it well past the state this file last recorded, so RE-READ it rather than trusting anything here about its progress. Item 4 (250-writingsystem-activation) needs an API-surface policy decision and is the item most likely to end needs_human. Do not start either without explicit user authorisation. The SHARED-CLONE GIT PROTOCOL in hard_rules is binding on both.",
  "items_done": [
    {
      "index": 1,
      "slug": "243-closeproject-save-guard",
      "issues": [
        243
      ],
      "closed": "2026-09-07, spurt 9 / cycle 9, feature_complete APPROVED by lex-lead",
      "outcome": "#243's filed loss chain FIXED and measured 0/25 -> 25/25 survivors, in memory and on disk, re-read from the LCM after close-and-reopen. Two guards: SaveChanges() refuses at CurrentDepth > 0 with FP_TransactionError before usm.Save() is attempted (C21, a PUBLIC BEHAVIOUR CHANGE), and CloseProject()'s End mirror can no longer skip usm.Save() (C1/C6/C7). Contract C1-C30 frozen. Commits 8629d25, f3a0f50, 702f96b, 9dfd55d, f19304e, 0c82535, b0e3d14.",
      "not_claimed": "The Target.fwdata replacement by the crash-recovery copy is UNEXPLAINED and NOT fixed (C10) -- nothing in the fix surface could cause or prevent it. One narrow residual is unmeasured: a state where the depth read raises while ObjectRepository() and usm.Save() both still succeed at depth > 0 (C28), claimed neither safe nor a bug. All measurements are single-client on a target_sandbox_path copy; no shared/multi-client recovery behaviour was measured.",
      "github": "flexicon#243 was ACCIDENTALLY AUTO-CLOSED on 2026-09-07T19:25:15Z and has been REOPENED (state REOPENED) by the main session on 2026-09-08. Cause: commit b0e3d14's SUBJECT read \"close #243's crew review (T9)\" -- meaning close the crew REVIEW -- which GitHub parsed as the auto-close keyword \"close #243\". The ruling below stood and still stands; it was defeated mechanically, not overturned. ORIGINAL RULING, still in force: deliberately left OPEN, the crew files and closes nothing; lex-lead recommends keeping it open to carry the unexplained .fwdata half rather than closing and depending on a follow-up being written. That .fwdata half (C10) is still unfixed. LESSON: a possessive or descriptive use of close/fix/resolve + #N still fires -- see also #242 below."
    },
    {
      "index": 2,
      "slug": "242-paragraph-whitespace",
      "issues": [
        242
      ],
      "closed": "2026-09-07, cycle 4, feature_complete APPROVED by lex-lead",
      "outcome": "Four writers (ParagraphOperations Create/SetText/InsertAt, SegmentOperations AppendSentence) validated emptiness on a .strip()ed copy then persisted that stripped copy. They now validate on a throwaway and persist the caller's original bytes; 8 payloads confirmed round-tripping byte-for-byte through all four writers, the layer-B raw-MakeString bypass, and in-memory/on-disk. Whitespace-only input still raises FP_ParameterError at all four, verbatim messages, no write before the raise. AppendSentence's non-str branch no longer strips, harmonising str/non-str. C12 additionally fixed a PRE-EXISTING AppendSentence join-boundary defect exposed (not introduced) by the whitespace fix: the terminator was inserted past a paragraph's trailing whitespace ('foo ' + 'bar' gave 'foo . bar'; 'foo.  ' gave the double terminator 'foo.  . bar'). It now anchors at the last non-whitespace char and reuses the existing whitespace as the separator ('foo. bar', 'foo.  bar'), deleting ZERO characters, and is provably inert whenever trailing whitespace is absent -- i.e. on every input reachable before this fix. 9/9 predictions matched, zero misses. Independent gate GREEN, zero P0s, all six checks re-derived from source and git history. Contract C1-C14 frozen.",
      "not_claimed": "The owner's field figures (44/104 paragraph contents, 41/86 segment baselines, Ejagham Mini -> Target) were NOT reproduced; Checkpoint 4 was DECLINED, not deferred (C13). All verification is single-client against target_sandbox copies. The 8 sibling sites (Q-242A), CheckOperations' total non-str payload loss (Q-242B) and coercion policy (Q-242C) are untouched.",
      "github": "flexicon#242 was AUTO-CLOSED on 2026-09-07T19:25:16Z (state CLOSED/COMPLETED) by commit 066bab0, whose body carried \"closes #242\". This CONTRADICTED the policy recorded here, which was \"left as-is, the crew files and closes nothing\". Verified 2026-09-08: the close event's commit_id is 066bab0, 066bab0 is an ancestor of origin/main, and origin/main..main is 0 commits -- so the keyword genuinely fired and the never-pushed escape hatch does not apply. LEFT CLOSED PENDING A USER DECISION, deliberately not reversed: the defect as FILED (whitespace stripping at the four writer sites) is genuinely fixed and twice-verified, so COMPLETED is defensible for the issue as written. THE RISK is that the deliberately-descoped residuals -- Q-242A (spec.md C10), Q-242B (C10/C6 item 3, flagged Tier-1 silent data loss on its own merits) and Q-242C (C11) -- are now tracked ONLY in this campaign's QUEUE.md, which is exactly the \"closing and depending on a follow-up being written\" failure the #243 ruling warned against. The user must either file those three as issues and leave #242 closed, or reopen #242 to carry them. NOT filed by any agent: QUEUE.md records them as awaiting user approval."
    },
    {
      "index": "2a",
      "slug": "name-field-whitespace-identity",
      "issues": [
        242
      ],
      "covers": [
        "Q-242A",
        "Q-242B"
      ],
      "closed": "2026-09-07, cycle 6, APPROVED by lex-lead (FEATURE COMPLETE for the sub-item only)",
      "outcome": "All 8 name-field persist sites and all 3 sibling comparison sites fixed across four Operations classes. Writers persist the caller's original bytes; comparisons strip BOTH sides. The two halves are inseparable (C1/C2): the writers' strip was load-bearing for dedup coherence, so a persist-only fix would have let Create() mint unbounded duplicate-looking records instead of raising. Q-242B additionally converted a total payload loss (non-str OR whitespace-only silently coerced to '' and persisted) into a loud TypeError/FP_ParameterError. Live: 20/20, run_mode live, target_sandbox only. Offline passed unchanged at 1292 throughout. Contract C1-C13 frozen. Commits 3eac4a0, db95230, ab638aa, 0ab9c60, 90cfce7, 5c161e1, plus spec/state commits.",
      "not_claimed": "8/8 is NOT claimed and was never claimed. DiscourseOperations.CreateChart's persist half is FAIL: unverified (Q-DISC1) -- inspection-correct, and padded vs unpadded inputs were measured to fail IDENTICALLY (ruling out the edit as the cause), but the method is unreachable through its own public API for two pre-existing unrelated defects, and is empirically not workaroundable at the harness level. Q-242D is a disclosed remaining gap: whitespace-only and empty names are still silently persisted at AnthropologyOperations.Create/CreateSubitem and TextOperations.SetName. Reaching CreateCheckType at all required a test-instance monkeypatch of the broken _GetCheckList stub (Q-CHK1), disclosed in both evidence files under a mandatory WHAT WAS NOT EXERCISED section.",
      "github": "flexicon#242 is CLOSED -- see queue item 2's github field for the auto-close and the pending user decision; this sub-item did not close it and files nothing. The two residuals this sub-item covers were filed by the OTHER crew on origin/main (PR #282) and are now tracked as issues in their own right: Q-242A as flexicon#274 and Q-242B as flexicon#273. Reconciled at the 2026-09-08 merge of origin/main; the earlier text here read 'flexicon#242 deliberately left OPEN', which was the recorded policy but is not the tracker's actual state."
    }
  ],
  "queue": [
    {
      "index": 1,
      "slug": "243-closeproject-save-guard",
      "issues": [
        243
      ],
      "status": "done",
      "spec_exists": true
    },
    {
      "index": 2,
      "slug": "242-paragraph-whitespace",
      "issues": [
        242
      ],
      "status": "done",
      "spec_exists": true,
      "note": "User authorised 2026-09-07. Four writers strip leading/trailing whitespace but persist the STRIPPED value: ParagraphOperations.py:171 (Create), :576 (SetText), :716 (InsertAt), SegmentOperations.py:589 (AppendSentence). The strip is only needed for the emptiness check. Owner field evidence, Ejagham Mini -> Target: 44 of 104 paragraph contents and 41 of 86 segment baselines DIFFERED, every difference lost trailing whitespace. Irony on the record: Create and AppendSentence gained guid= in #239 specifically so objects could be reproduced faithfully -- they now preserve identity perfectly but still cannot preserve the text. Owner suggested fix: validate on a stripped copy, write the caller's original. Owner also offered a preserve_whitespace kwarg as an alternative and offered to open a PR. CHECK Shared/string_utils.normalize_text for null-marker ('***') interaction before ruling on the shape.",
      "sub_items": [
        {
          "index": "2a",
          "slug": "name-field-whitespace-identity",
          "issues": [
            242
          ],
          "covers": [
            "Q-242A",
            "Q-242B"
          ],
          "status": "done",
          "spec_exists": true,
          "note": "Spun out AFTER item 2 closed, as the Q-242A/Q-242B execution vehicle. Authorised by the user 2026-09-07. Closed cycle 6, APPROVED."
        }
      ]
    },
    {
      "index": 3,
      "slug": "feature-structure-sync-gap",
      "issues": [
        251,
        252,
        253,
        256
      ],
      "status": "queued",
      "spec_exists": true,
      "note": "NOT AUTHORISED. RESUME, do not re-plan. Contract C1-C8 frozen, rulings D1-D5 settled, spurt 1 complete. Its own .crew-handoff.json next_entry is authoritative: T1 ALONE (13 owner classes into lcm_casting._interface_cache + caller-delta enumeration + green offline suite and NC/Phoneme live tests) before T2."
    },
    {
      "index": 4,
      "slug": "250-writingsystem-activation",
      "issues": [
        250
      ],
      "status": "queued",
      "spec_exists": false,
      "note": "NOT AUTHORISED. LAST on purpose -- needs an API-surface policy decision, so it is the item most likely to end needs_human. Must cover both routes into the silent skip: store-vs-active AND case/separator normalization. Note the cycle-6 lex-domain finding on item 1: writing-system add/remove/change is one instance of a broader class of schema/metadata-mutating operations, which is itself a separate queued approval ask -- this item is the instance, not the class audit."
    },
    {
      "index": "2a",
      "slug": "name-field-whitespace-identity",
      "issues": [],
      "status": "active",
      "spec_exists": false,
      "note": "AUTHORISED by the user 2026-09-07 ('use subagent to fix the 12 sites with the bug'). Spun out of item 2: Q-242A's 8 sibling sites plus Q-242B. Bucket 1 held 12; 4 were #242 itself, closed at 249863d, so the authorised remainder is 8. NO GitHub issue exists and none may be filed. lex-lead ruled C1-C8: dedup stays whitespace-insensitive but must be made EXPLICIT and SYMMETRIC, because all three comparisons strip the needle and never the haystack -- a persist-only fix therefore yields UNBOUNDED DUPLICATES of a name byte-identical to itself, not the duplicate-rejection #242 C10 predicted (C2 corrects C10). USER OVERRIDE 2026-09-07: the user directed 'do what is best for the user, /lex-domain can decide', so lex-domain's verdict GOVERNS the C3 identity question rather than merely advising; lex-lead's C3 stands only if lex-domain accepts it. Q-242B rides along as its own task at its own severity, fixed by adopting the already-shipped _ValidateStringNotEmpty; Q-242C stays UNAUTHORISED and untouched. Shared code (BaseOperations.py, Shared/string_utils.py) is fenced off -- needs_human if either must change."
    }
  ],
  "hard_rules": [
    "Live LCM verification REQUIRED for write-path changes; mock-only is FAIL: unverified.",
    "target_sandbox / target_sandbox_path fixtures ONLY. Never the real Target, never scripts/restore_*.py, unattended -- stop with needs_human instead.",
    "Never run bare pytest or pytest --ignore=tests/contract; always FLEXLIBS_REQUIRE_LIVE=1 with -m requires_live_project and an explicit test file.",
    "Derive every live suite's marker count with --collect-only -q -m requires_live_project and paste it into the evidence file. NO TESTS COLLECTED IS A ZERO, NEVER A PASS. (Added spurt 3, after a named gate was found to carry zero markers.)",
    "Offline baseline is 1292 passed as of b0e3d14. Any delta is explained, not absorbed.",
    "Relay specialist report PATHS + 2-line summaries, never bodies.",
    "One checkpoint per dispatch, then update state, commit, stop.",
    "Do not file new GitHub issues inside crew work; append them to QUEUE.md 'Awaiting user approval'.",
    "A task that states an a-priori prediction commits it BEFORE the measuring run, so temporal order is provable from history (C28 forward rule).",
    "No spurt may close with a top-of-file state summary that contradicts the record (C27). This campaign-level .crew-handoff.json is the MAIN SESSION's to maintain -- it went stale at item 1's closure (left reading spurt 5 / needs_human / active_index 1) and was rewritten by the main session on 2026-09-07. Doc agents are explicitly out of scope for it; whoever closes a queue item must update it in the same pass.",
    "No emoji in console output (Windows).",
    "A rise in the offline DESELECTED count equal to the number of newly added requires_live_project tests is expected and needs only the new tests named; a change in the offline PASSED count always requires explanation. Baseline: 1292 passed (483 deselected as of ed428f7).",
    "SHARED-CLONE GIT PROTOCOL -- promoted verbatim from specs/name-field-whitespace-identity/CONCURRENCY.md per that feature's contract item C12(d) as extended by C12(f), which requires AMENDMENT 2 and AMENDMENT 3 to be promoted as ONE UNIT and not separately: AMENDMENT 2 brackets the commit, AMENDMENT 3 closes the working-tree-destruction gap the bracket does not reach. BINDING on campaign items 3 and 4, which run in the SAME shared clone against the SAME second crew. EVIDENCE BASE: the staging race fired THREE times during sub-item 2a -- cycle 3 (T3, caught AFTER the bad commit by step 4, needed a rewind), cycle 4 (T4, caught BEFORE the commit by step 3, no rewind), and cycle 6 (lex-lead itself, clean at step 3 and still hit, caught by step 4). TWO of the three landed a foreign stage after a clean step-3 check, so step 3 and step 4 are each insufficient alone and neither may be skipped. Verbatim text follows.\n\n## AMENDMENT 2 (2026-09-07, cycle 3) -- the staging window is a RACE; check at COMMIT time too\n\n**Binding from T4 onward. This STRENGTHENS the staging rule above; it does not\nreplace it. Authored by `/lex-lead` at the cycle-3 checkpoint, on `/lex-programmer`\n(T3)'s recommendation, after a real incident.**\n\nDuring cycle 3, T3's first `git commit` swept in five of the other crew's files\n(`flexicon/code/BaseOperations.py`, two `Grammar/` files, two\n`specs/feature-structure-sync-gap/` files). **T3 had followed the rule exactly as\nwritten**: it ran `git status --porcelain` before its `git add`, and the tree was\nclean at that instant. The other crew's own `git add` landed **in the window\nbetween that check and T3's commit**, so their paths were already sitting in the\nshared index by commit time. The pre-`add` check is structurally incapable of\ndetecting this. It is a race, not a lapse of discipline -- and it will recur.\n\nT3 self-caught it via `git show --stat HEAD`, corrected with `git reset --soft\nHEAD~1` followed by an index-only `git reset HEAD -- <foreign paths>` (zero\nworking-tree bytes touched, verified by diff before and after), and re-committed\nclean. `/lex-lead` independently audited the resulting history and confirms it is\nclean: `7bc6d01` contains only its own evidence file, and the other crew's work\nlanded separately as their own commit `6643b48`.\n\nBecause the index is shared and they can stage at any instant, **every commit made\nwhile they are active must be bracketed on both sides**:\n\n1. `git status --porcelain` immediately BEFORE `git add` (the existing rule).\n2. `git add <explicit paths>` -- never `-A`, never `.`, never `-u`, never\n   `git commit -a`.\n3. **`git status --porcelain` AGAIN immediately BEFORE `git commit`.** Read the\n   STAGED (first-column) entries specifically. If any staged path is one you did\n   not author, run `git reset HEAD -- <that path>` **before** committing. Never\n   `git checkout` / `git restore` / `git stash` it -- that destroys their\n   unrecoverable uncommitted work.\n4. **`git show --stat HEAD` immediately AFTER `git commit`.** Confirm the file\n   list is exactly what you intended and nothing more.\n5. If a foreign path did get committed anyway: `git reset --soft HEAD~1`, then\n   index-only `git reset HEAD -- <foreign paths>`, then re-commit. `--soft` and\n   an index-only `git reset HEAD -- <path>` are the ONLY two reset forms\n   authorised in this clone; both touch zero working-tree bytes. **`git reset\n   --hard` is FORBIDDEN here while the other crew is active**, as is any rewrite\n   of a commit that is not yours.\n6. **Report the outcome of steps 3 and 4 in your task report, even when clean.**\n   A line of the form \"no foreign paths staged at commit time; `git show --stat\n   HEAD` lists only <N> files, all mine\" is REQUIRED, not optional. Silence is\n   read as \"not checked\".\n\nSteps 3 and 4 cost seconds and catch the only failure mode the pre-`add` check\ncannot. Skipping them is a protocol violation **regardless of whether the commit\nhappened to come out clean** -- T3's incident is proof that a clean pre-`add`\ncheck predicts nothing about the index a moment later.\n\n## AMENDMENT 3 (2026-09-07, cycle 4) -- `git stash` is authorised ONLY with an explicit pathspec\n\n**Binding from T5 onward. Authored by `/lex-lead` at the cycle-4 checkpoint,\nmirroring `spec.md` C13(c). This CLOSES A GAP in the staging rule above; it\ndoes not contradict it.**\n\nThe staging rule already forbids stashing **a path you did not author**. It did\nnot say, in terms, that a **bare** `git stash` is forbidden -- and AMENDMENT 2\nstep 5's \"only two authorised forms\" clause is scoped to `git reset` forms, not\nto `git stash`. A reader had to derive the prohibition. State it directly:\n\n- **`git stash push -- <your own path>` is the ONLY authorised stash form in\n  this clone.**\n- **A bare `git stash` is FORBIDDEN, at the same level as `git reset --hard`**,\n  as are `git stash -u`, `git stash --all`, and `git stash push` with no\n  pathspec. All of them operate on the WHOLE working tree, which here means the\n  other crew's uncommitted, unrecoverable in-flight work would be swept into a\n  stash they do not know exists.\n- Stash only paths you authored. Never stash, `git checkout`, or `git restore` a\n  path you did not author -- unchanged from the staging rule above.\n- After any stash, confirm **`git stash list` is EMPTY** before you finish, and\n  report that it is. Never carry a stash across a checkpoint or a handoff.\n- Do not use a stash to \"tidy\" the working tree. Its only authorised use here is\n  the narrow one in C13(d): recovering a correct pre-edit offline baseline after\n  an ordering slip, when the baseline was not taken first as required.\n\nOccasioned by T4 (cycle 4), which used a correctly pathspec'd stash to recover a\ngenuine pre-edit baseline -- the safe form -- and self-disclosed it. The delta\nwas accepted (`spec.md` C13(b)). This amendment exists so that the NEXT agent,\nreading only that a stash-based recovery was accepted, does not reach for the\nbare form.\n\n**Authorised destructive-adjacent git operations in this clone, consolidated:**\n`git reset --soft HEAD~1`; index-only `git reset HEAD -- <path>`;\n`git stash push -- <your own path>`. **Everything else is FORBIDDEN while the\nother crew is active** -- including `git reset --hard`, bare `git stash`,\n`git checkout`/`git restore` of any path, and any rewrite of a commit that is\nnot yours."
  ],
  "spurts_completed": 17,
  "blocker": null,
  "campaign_log_pointer": "specs/tier1-silent-data-loss/QUEUE.md -> 'Campaign Log'",
  "deferred_needs_user_approval_pointer": "specs/tier1-silent-data-loss/QUEUE.md -> 'Awaiting user approval'. As of item 1's closure this holds: (1) C25 -- does UndoableUnitOfWorkHelper.Dispose() + set_RollBack(True) actually discard anything? P-11 measured 25/25 SURVIVING an escaping exception, contradicting undoable_operation.py's own claim; (2) C29 -- transaction.py:146-153 records the OPPOSITE outcome for a created POS and is the record's only counter-measurement, with four uncontrolled variables between them; (3) the docs/TRANSACTION_GUIDE.md API-reference gap, gated behind (1); (4) the depth-guard-coverage audit of the broader schema-mutating class (lex-domain cycle-6 Q2), overlapping issue #250. Plus ONE ungated cleanup needing no approval: C30 -- bound test_transaction_honesty.py's source-slice windows by the next 'def ' rather than a magic width; do NOT widen 6000 to 8000. Item 2 added three more: Q-242A (8 sibling validate-stripped/persist-stripped sites, blocked on a dedup-identity ruling), Q-242B (CheckOperations non-str TOTAL payload loss -- Tier-1 in its own right, filed separately and more severely so it is not triaged at whitespace severity), Q-242C (str/non-str coercion policy harmonisation). Item 2 also DECLINED, rather than deferred, the corpus-shaped reproduction of the owner's 41/86 figure (C13): the mechanism was proven directly at unit level, so the field figure is NOT claimed as reproduced. CORRECTION APPENDED 2026-09-07 (contract item C10(b), frozen in specs/name-field-whitespace-identity/spec.md): the Q-242B severity wording above is OVER-STATED in one direction and UNDER-STATED in another, and both halves matter. (1) OVER-stated: CheckOperations._GetCheckList() is a hardcoded stub that forces _GetOrCreateCheckList() down a branch calling a non-existent ServiceLocator.GetInstance, so CreateCheckType() raises AttributeError on EVERY call for EVERY payload -- the silent empty-name persist is MASKED and no user has ever reached it. It is not loss users have suffered, and must not be advertised as such. That stub is recorded separately as Q-CHK1 (two defects: the GetInstance/GetService typo AND an unowned-list design gap) and is UNAUTHORISED. (2) UNDER-stated: the defect is reachable by an ordinary str, not only a non-str -- CreateCheckType('   ') strips to '' and persists an empty name with no exception. Sub-item 2a also added Q-CHK1, Q-DISC1 (DiscourseOperations.CreateChart is unreachable through its own public API for two pre-existing defects, so this feature's C8 persist pin there is FAIL: unverified) and Q-242D (whitespace-only names silently persisted at the three _ValidateParam-only sites; deliberately left to Q-242C rather than fragmenting that decision). SEQUENCING RULE added at sub-item 2a's closure: Q-242C (validator harmonisation across differing exception types) now has TWO dependents and must be decided BEFORE either -- (a) Q-242D, the whitespace-only/empty silent persist at AnthropologyOperations.Create/CreateSubitem and TextOperations.SetName, and (b) the three Raises docstring blocks corrected at those same sites in commit 5c161e1, which document today's non-rejecting behaviour and will need re-correcting if Q-242C adds the rejection. Deciding Q-242D or rewriting those docstrings ahead of Q-242C would fragment one decision across three places. OBSERVATION, no approval needed and not a blocker: this repo has no docs/MANIFEST.md; lex-doc correctly declined to bootstrap one from inside a narrowly dispatched docs task rather than silently expanding scope. Worth a standalone doc-audit invocation someday.",
  "campaign_progress": "2 of 4 queue items done (#243, #242), plus spun-out sub-item 2a (name-field-whitespace-identity, covering Q-242A and Q-242B) DONE and APPROVED. NO ITEM IS ACTIVE. Items 3 and 4 are untouched and NOT AUTHORISED; each needs explicit user authorisation before any work begins.",
  "code_landed_so_far": "flexicon/code/FLExProject.py (CurrentDepth, HasOpenSessionTask(), _ReadActionHandlerDepth(), the SaveChanges() depth guard, the CloseProject() End-guard plus ERROR log and try/finally Dispose), flexicon/code/undoable_operation.py (P-11 measured caveat, docstring only). Item 2 added flexicon/code/TextsWords/ParagraphOperations.py (Create/SetText/InsertAt: validate on a throwaway strip, persist the original) and flexicon/code/TextsWords/SegmentOperations.py (AppendSentence: same, plus the C12 join-boundary terminator anchor). Nothing else under flexicon/ has been modified by this campaign. Sub-item 2a added flexicon/code/TextsWords/DiscourseOperations.py (T1, Shape A), flexicon/code/TextsWords/TextOperations.py (T2, Shape B at SetName + symmetric-strip Exists), and flexicon/code/Notebook/AnthropologyOperations.py (T3, Shape B at Create/CreateSubitem + symmetric-strip Find). flexicon/code/System/CheckOperations.py (T4, 0ab9c60 -- CreateCheckType/FindCheckType/SetName: _ValidateStringNotEmpty with no reassignment, per C7, closing BOTH Q-242A and Q-242B at the same three expressions). T1 is 3eac4a0, T2 db95230, T3 ab638aa. Docs landed at T5 (90cfce7: two [Unreleased] CHANGELOG entries + 11 docstrings) and the cycle-6 micro-fix (5c161e1: three proven-false Raises blocks corrected; zero executable lines). Nothing else under flexicon/ has been modified by this campaign.",
  "github_issues_filed": [],
  "updated": "2026-09-07"
}
