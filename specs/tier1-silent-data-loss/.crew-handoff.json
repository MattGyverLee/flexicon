{
  "campaign": "tier1-silent-data-loss",
  "kind": "campaign",
  "status": "needs_human",
  "completion_promise": "TIER1 COMPLETE",
  "promise_condition": "all four queue items status=done, gates green, live evidence on file. A per-feature feature_complete from lex-lead does NOT satisfy this.",
  "active_index": 1,
  "active_feature": "243-closeproject-save-guard",
  "next_entry": "STOP -- DO NOT RESUME AUTONOMOUSLY. Item 1 (243-closeproject-save-guard) is needs_human as of spurt 4. CP-B PASSED: the P0 CloseProject() End-guard is landed in flexicon/code/FLExProject.py (+52) and live-verified (probe 6/6, test_undoable_mode_live.py 33/33, test_target_live_smoke.py 3/3, run_mode: live; test_transaction_rollback.py 20/20 offline; offline suite unchanged at 1290 passed), and P-3 -- the loss mechanism -- is FIXED 0/25 -> 25/25 with no raise. Acceptance criteria 1-4 satisfied. BUT the owner's ACTUAL sequence (P-5) measured 0/25 even with the guard, and spec.md C14 found the guard makes that path SILENT where it previously raised. The remaining work (T7) is a public-behaviour change coupled to the still-unruled SaveChanges() depth guard, so the user must rule on BOTH together. See specs/243-closeproject-save-guard/.crew-handoff.json 'blocker' -- it is authoritative. Item 1's task order is now T6 -> T7 -> T5 (T5/CHANGELOG is LAST). ONLY T6 is pre-authorised without the ruling (probe-only, target_sandbox_path fixture, no flexicon/ behaviour change); it sharpens the ruling by settling whether the data is already gone before CloseProject() is entered, and by finding T7's detector. Do NOT enter items 2, 3 or 4 to keep the loop busy -- the queue is strictly ordered and item 1 is unfinished. Do NOT file GitHub issues. Do NOT implement or prototype the SaveChanges() guard. Do NOT emit TIER1 COMPLETE -- three of four items are untouched.",
  "queue": [
    {
      "index": 1,
      "slug": "243-closeproject-save-guard",
      "issues": [
        243
      ],
      "status": "needs_human",
      "spec_exists": true,
      "note": "Spec+probe DONE (spurt 1). CP-A1/T1 DONE (spurt 2, purely additive P1 depth-read surface, live-verified). CP-A2/T2 DROPPED ON THE MERITS (spurt 3, spec.md C12) with zero lines of flexicon/ changed. **CP-B/T3+T4 DONE AND PASSED (spurt 4)**: the P0 guard is landed (+52 in FLExProject.py) and live-verified with run_mode: live; P-3, the loss mechanism C9 names, is FIXED 0/25 -> 25/25 with no raise; P-4 control unchanged 25/25; offline suite unchanged at 1290 passed; acceptance criteria 1-4 satisfied; SaveChanges() provably unmodified. **NOW needs_human.** P-5 -- the owner's ACTUAL sequence -- measured 0/25, and /lex-lead ruled rather than accepting the report's framing: C13 (the 'guard skipped an End that should have run' hypothesis is DISPROVED by measurement, so T3 needs no rework and C1/C6/C7 stand; but the asserted UnitOfWorkService internals mechanism is NOT frozen -- three rival mechanisms fit, and one of them would mean no CloseProject()-side change could ever reach 25/25, so it goes to new probe task T6); C14 (the guard turned 'lost + RAISED' into 'lost + returns normally with one debug line' on the owner's path -- a NEW silent-loss surface, and an observability regression on the exact complaint filed; the INSTRUCTION was wrong, not the implementation, same class as C11; remedy is new task T7, which restores LOUDNESS not DATA); C15 (Q2(a) RESOLVED, forced by C14 -- Dispose() moves into a try/finally). The cycle-4 note filed under spec.md Q2 was ruled MISFILED and moved to a new Q5. Two CP-B defects found in review that the report did not raise: the debug-level branch, and P-5's 'CloseProject() did not raise' being unasserted and unquoted (load-bearing for C13 fact 3 -- pinned by T6). TASK ORDER CHANGED: T6 -> T7 -> T5, with T5 (CHANGELOG) LAST; T6/T7 appended rather than renumbered so existing cross-references stay valid. ONLY T6 is pre-authorised without the user's ruling. Contract C1-C15 frozen. Q1 CLOSED (C9/C10), Q2 CLOSED (C15), Q3 CLOSED (no capability token); Q4 (CHANGELOG wording, at T5) and Q5 (the no-op-save DETECTOR, at T6) open. Standing overrides of the issue prose unchanged: the reorder fix is DISPROVED (C1), End-then-Save order stays (C7), P1 was a hard prerequisite of P0 (C6). LIVE-SUITE RULE honoured this spurt -- all four marker counts derived with --collect-only and pasted into the evidence file."
    },
    {
      "index": 2,
      "slug": "242-paragraph-whitespace",
      "issues": [
        242
      ],
      "status": "queued",
      "spec_exists": false,
      "note": "Check whether the stripping is load-bearing for the FLEx null-marker path (Shared/string_utils.normalize_text) before changing behaviour."
    },
    {
      "index": 3,
      "slug": "feature-structure-sync-gap",
      "issues": [
        251,
        252,
        253,
        256
      ],
      "status": "queued",
      "spec_exists": true,
      "note": "RESUME, do not re-plan. Contract C1-C8 frozen, rulings D1-D5 settled, spurt 1 complete. Its own .crew-handoff.json next_entry is authoritative: T1 ALONE (13 owner classes into lcm_casting._interface_cache + caller-delta enumeration + green offline suite and NC/Phoneme live tests) before T2."
    },
    {
      "index": 4,
      "slug": "250-writingsystem-activation",
      "issues": [
        250
      ],
      "status": "queued",
      "spec_exists": false,
      "note": "LAST on purpose. Needs an API-surface policy decision, so it is the item most likely to end needs_human. Must cover both routes into the silent skip: store-vs-active AND case/separator normalization."
    }
  ],
  "hard_rules": [
    "Live LCM verification REQUIRED for write-path changes; mock-only is FAIL: unverified.",
    "Prefer the target_sandbox fixture. Never write to the real Target, and never run scripts/restore_*.py, unattended -- stop with needs_human instead.",
    "Never run bare pytest or pytest --ignore=tests/contract; always FLEXLIBS_REQUIRE_LIVE=1 with -m requires_live_project and an explicit test file.",
    "Relay specialist report PATHS + 2-line summaries, never bodies.",
    "One checkpoint per iteration, then update state, commit, stop.",
    "Do not file new GitHub issues inside the loop; append them to QUEUE.md 'Awaiting user approval'.",
    "No emoji in console output (Windows).",
    "Never name a live test suite by filename. Derive it with '-m requires_live_project --collect-only -q' and paste the count into the evidence file. A 'no tests collected' result is a ZERO, never a pass -- reporting it as green is a false verification claim. (Added spurt 3 after test_transaction_rollback.py, named as a live suite in the T2 brief, was found to carry zero live markers.)",
    "A specialist returning a FINDING instead of a diff is a legitimate outcome, not a failure to route around. Rule on it, freeze the ruling as a contract decision in spec.md, and mark the task DROPPED in tasks.md with the evidence path so a post-context-reset spurt cannot re-attempt it.",
    "A guard that stops an exception must not also stop the SIGNAL. When a fix converts a raise into a swallowed condition on a data-loss path, ask what the caller now sees and rule on it explicitly -- 'no longer raises' is not the same as 'no longer loses'. Added spurt 4 after spec.md C14: the #243 guard turned the owner's path from 'lost + raised' into 'lost + returns cleanly, one debug line', and 'log at debug level' in the task brief was the defect. Log anomalous states at ERROR, and prefer 'attempt everything, then fail loudly' over a quiet success.",
    "Do not freeze an inferred INTERNALS mechanism as a contract decision on the strength of one black-box measurement. Enumerate the rival mechanisms, state which observations would distinguish them, and route it to a probe. Freeze only what was measured, plus inferences whose premises are named. Added spurt 4 (spec.md C13)."
  ],
  "spurts_completed": 4,
  "items_done": [],
  "blocker": "ONE USER DECISION, TWO COUPLED PARTS, blocking item 1: (a) does FLExProject.SaveChanges() get a CurrentDepth guard so it fails fast instead of calling usm.Save() at depth > 0 (the campaign's fourth ask, in QUEUE.md 'Awaiting user approval' since spurt 1, unruled for three spurts)? and (b) given that answer, should CloseProject() raise when it detects a save it cannot trust (spec.md C14 / task T7)? They interact directly: approve (a) and the envelope is never collapsed, the P-5 chain never forms, the owner's sequence can reach 25/25, and T7's raise is near-unreachable defensive code; decline (a) and T7's raise is the ONLY signal the owner gets, so its severity and wording matter a great deal. Why blocking rather than merely queued: item 1's T4 measured the owner's own sequence at 0/25 survivors WITH the #243 guard in place, so #243's three asks do not fix the incident #243 was filed about -- closing it now would be a false completion by this campaign's own standard, and T5's release note cannot be written without the ruling. Good news for the decision: all three rival mechanisms in spec.md C13 imply the SAME shape for (a) -- refuse usm.Save() at CurrentDepth > 0, failing fast before any damage -- so the open mechanism question does not block approving it.",
  "updated": "2026-09-07",
  "campaign_log_pointer": "specs/tier1-silent-data-loss/QUEUE.md -> 'Campaign Log'",
  "deferred_needs_user_approval_pointer": "specs/tier1-silent-data-loss/QUEUE.md -> 'Awaiting user approval'. Spurt 1 appended one new item: SaveChanges() has no CurrentDepth guard, so under undoable=False it raises 'Commit at wrong place.' and collapses the session envelope -- reachable from its own shipped docstring Example. Fixing its BEHAVIOUR is a fourth ask beyond #243's three and needs a user ruling; the docstring correction is already in scope as item 1's T5.",
  "campaign_progress": {
    "item_1_243": "needs_human -- CP-A closed (T1 landed, T2 dropped); CP-B/T3+T4 DONE AND PASSED, the P0 guard is live-verified and P-3 is fixed 0/25 -> 25/25. Blocked at T6/T7: the owner's own P-5 sequence still measures 0/25 (C13) and the guard made that path silent (C14), so honestly closing #243 needs the user's coupled ruling on the SaveChanges() depth guard + C14's CloseProject() raise. T6 (probe) is pre-authorised; T7 and T5 are not.",
    "item_2_242": "queued -- untouched",
    "item_3_feature_structure_sync_gap": "queued -- untouched by spurts 1, 2 and 3; do not enter it",
    "item_4_250": "queued -- untouched, deliberately last"
  },
  "code_landed_so_far": [
    "flexicon/code/FLExProject.py -- T1 additive P1 depth-read surface (_ReadActionHandlerDepth, CurrentDepth property, HasOpenSessionTask). 120 insertions, 0 deletions. Live-verified, run_mode: live.",
    "SPURT 3: nothing. T2 dropped (C12); git diff f3a0f50 -- flexicon/ is empty and the offline suite is unchanged at 1290 passed.",
    "SPURT 4 (T3): flexicon/code/FLExProject.py +52 -- the P0 CloseProject() End-guard (HasOpenSessionTask() check, then try/except around EndNonUndoableTask(), so no raise there can skip usm.Save(); lines 326/332 order unchanged per C7). Live-verified, run_mode: live, target_sandbox_path fixture only. P-3 fixed 0/25 -> 25/25. SaveChanges() untouched. Also (T4) tests/operations/test_issue243_closeproject_probe.py extended in place. Record note: the spurt's diff also contains .claude/ralph-loop.local.md (+8/-76), the ralph-loop plugin overwriting its own stale scratch state from an unrelated old loop -- not flexicon/, not a spec artifact, harmless."
  ],
  "github_issues_filed": []
}
