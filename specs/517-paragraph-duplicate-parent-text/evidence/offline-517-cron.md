# Offline evidence -- issue #517 (cron)

**Command:**

```
python -m pytest tests/operations/test_issue517_paragraph_duplicate_parent_offline.py -m "not requires_live_project" -q
```

**Result:** (filled after run)

**run_mode:** N/A (offline)

**Pre/post LCM:** N/A

**Pass/fail:** (filled after run)
