# Issue #508 -- offline evidence (cron)

**Command:**

```
python3 -m pytest tests/operations/test_issue508_text_resolver_offline.py -q --noconftest
```

**Result:** PASS (4/4)

**run_mode:** mock (cloud agent; no FieldWorks live stack)
